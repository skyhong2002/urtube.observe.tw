# 2026-09-19 前端與品質稽核交付

基準：`cd23ce450a971ff0974367384188e77276838fef`。本次 PR 改善註冊、個人檔案、匯入、鍵盤導覽與 CI。後端行為依專案規範另行核准；本 PR 未調整認證、資料庫、配對、worker、模型預算或部署設定。

## 修正清單

| ID | 問題 | 改善 |
| --- | --- | --- |
| F01 | 註冊 ID 的 HTML pattern 使用未跳脫連字號，與現代瀏覽器的 Unicode Sets 規則不相容 | 修正 pattern，關閉自動大寫與拼字檢查 |
| F02 | 個人檔案名稱超過後端字數限制，仍能送出後才看到錯誤 | 依 Unicode 字元數檢查 1–80 字，保留既有伺服器驗證 |
| F03 | 編輯資料後誤離開頁面，草稿會直接遺失 | 有變更時使用瀏覽器離開提醒；正常儲存及還原原值不提示；不持久儲存個資 |
| F04 | Takeout 選錯檔案或超過 100 MiB，需等送出後才知道；匯入中缺乏提示 | 上傳前檢查、顯示處理訊息、防止重複送出，返回頁面後可重試 |
| F05 | 切換時間範圍時，請求卡住可讓載入狀態無限持續 | 15 秒後改用完整頁面導覽；已取消的請求不再導向 |
| F06 | 鍵盤使用者須逐一經過導覽列才能到主內容 | 新增中英文「跳到主要內容」，擴充原生控制項的焦點提示 |
| F07 | 減少動態效果偏好仍會觸發全站平滑捲動 | 停用該偏好下的平滑捲動 |
| F08 | 註冊可用性提示及部分表單錯誤不易被輔助技術發現 | 新增狀態區、錯誤 alert 與錯誤焦點 |
| F09 | 綁定帳號的私人存取金鑰顯示為普通文字 | 使用遮蔽輸入欄位 |
| F10 | 已有密鑰與依賴掃描未加入每次 PR 檢查 | CI 取得完整歷史後執行既有 security:audit |
| F11 | npm、Python 與 GitHub Actions 沒有定期依賴更新 PR 設定 | 加入每週 Dependabot 檢查，不啟用自動合併 |

HTML pattern 的規則參考 [MDN](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Attributes/pattern)。

## 驗證

- TypeScript 檢查通過。
- Node 測試：410 通過，0 失敗、0 略過，包含真正的 Node → Python HTTP 配對測試；資料全為虛構或暫存。
- Python 數值服務：15 項通過；部署 manifest 檢查通過。
- 新增回歸：範圍切換逾時／取消、草稿變更／還原／正常儲存／返回，以及上傳重複提交／返回恢復。
- 瀏覽器：320、390、1280 px 抽查無水平溢出；中英文註冊、非法 ID、名稱過長、101 MiB 測試檔案、Tab → Enter 跳到主內容、編輯後離開與還原。
- 既有 tracked-files／git-history 密鑰掃描通過。這只涵蓋高信心格式，不是所有憑證形式的保證。
- npm 掃描仍有 Hono 的一項中度套件告警（包含三份公告）。現有 gate 為 high，不會因中度告警阻止 CI；後端套件升級另行核准。掃描通過不可解讀為零漏洞。
- Python 依賴解析結果未發現已知漏洞；解析時間為本次稽核，不能取代正式映像 SBOM。
- 正式站僅做一般 GET：`/`、`/signup`、`/privacy`、`/healthz`、`/readyz` 均 200；未執行破壞性或負載測試。

本機沒有運行 Docker daemon，映像建置交由 PR CI 驗證。本機 Node 為 26.5.0；CI／正式映像使用 Node 22，以 CI 結果為版本相容依據。

## 使用限制

瀏覽器離開提醒受瀏覽器及使用者互動規則限制，不能保證攔截手機作業系統直接關閉程序。前端上傳限制改善回饋，伺服器仍須獨立實施限制。這次測試不是完整 WCAG 認證、正式 OAuth 登入測試或正式站滲透測試。

後續的後端、效能、營運與產品決策清單由專案擁有者另行審閱。這份公開紀錄不含尚未修補項目的利用步驟。
